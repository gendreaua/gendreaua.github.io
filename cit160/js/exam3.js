
function makeFullName(givenName, familyName) {
    let givenName = ["Theodore", "Barak", "Donald"];
    let familyName = ["Roosevelt", "Obama", "Trump"];
    list1.forEach(myFunction);
        return givenName[i] + familyName[i];
    


    document.getElementById('output').innerHTML = list1.join + list2.join;
}

